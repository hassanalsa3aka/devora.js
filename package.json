{
  "name": "devora-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx packages/cli/src/index.ts dev",
    "build": "tsx packages/cli/src/index.ts build",
    "typecheck": "tsc -b --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest run"
  },
  "workspaces": ["packages/*", "apps/*", "adapters/*", "create-devora"],
  "dependencies": {
    "@devorajs/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@devorajs/cli": "*",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
